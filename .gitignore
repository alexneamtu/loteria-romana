.worktrees/
__pycache__/
*.pyc
/analysis_results/
/data/analysis/
/results/

# Scraped HTML caches: regenerated by every fetch, never committed by CI
# (the workflows only commit data/clean, the ledger, and results history).
/data/raw/
# Local SQLite results DB (RESULTS_DB_PATH); production uses DATABASE_URL.
/data/results/*.db
